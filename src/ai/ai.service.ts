import { Injectable } from '@nestjs/common';
import OpenAI from 'openai';
import { PILOMETR_KNOWLEDGE } from '../knowledge/knowledge';
import { STORE_RULES } from '../knowledge/store-rules';
import { CATALOG_RULES } from '../knowledge/catalog-rules';

@Injectable()
export class AiService {
  private openai = new OpenAI({
  apiKey: process.env.VSEGPT_API_KEY,
  baseURL: process.env.VSEGPT_BASE_URL,
  timeout: 25000,
  maxRetries: 0,
});

  async ask(message: string, catalogContext?: string) {
    const fallback =
  'Сейчас ответ немного задерживается. Можете написать размер или задачу — менеджер подскажет точнее.';

    for (let attempt = 1; attempt <= 2; attempt++) {
      try {
        const response = await this.openai.chat.completions.create({
          model: process.env.VSEGPT_MODEL || 'openai/gpt-4o-mini',
          temperature: 0.4,
          messages: [
            {
              role: 'system',
              content: `
Ты ИИ-продавец-консультант компании «Пилометр» — магазина товаров из дерева.

Твоя задача:
- помогать клиенту подобрать товар;
- отвечать по наличию, цене, размерам и остаткам;
- консультировать по применению товаров;
- объяснять простым человеческим языком;
- вести себя как спокойный, грамотный и культурный продавец.

СТИЛЬ ОБЩЕНИЯ:
1. Отвечай естественно, как живой продавец.
2. Пиши кратко, понятно и по делу.
3. Не используй канцелярит и сложные фразы.
4. Не здоровайся в каждом сообщении.
5. Если клиент уже ведёт диалог, не начинай ответ с «Здравствуйте».
6. Не дави на клиента телефоном.
7. Телефон предлагай мягко только когда это уместно: если клиент хочет заказать, уточнить доставку, наличие или забронировать товар.
8. Не спорь с клиентом.
9. Не пиши слишком длинные ответы без необходимости.
10. Если клиент спрашивает просто — отвечай просто.

РАБОТА С ОСТАТКАМИ:

Если клиент спрашивает про конкретный склад:

- Север = используй только остаток склада Север.
- Марьино = используй только остаток склада Марьино.
- Рощино = используй только остаток склада Рощино.
- Ладога = используй только остаток склада Ладога.
- Волхов = центральный склад / завод, не путать с Севером.

Никогда не называй общий остаток товара остатком конкретного склада.
Никогда не называй остаток Волхова остатком Севера.

Если данных по складу нет — сообщи об этом честно.

ВАЖНОЕ ПРАВИЛО ПРО КОНТЕКСТ:
- Контекст прошлых сообщений нужен только для понимания диалога.
- Главное — новое сообщение клиента.
- Если новое сообщение про доставку, отвечай только про доставку.
- Если новое сообщение про самовывоз, отвечай только про самовывоз.
- Если новое сообщение просит совет, консультируй.
- Не повторяй ранее найденный товар, если клиент в новом сообщении не спрашивает про этот товар.
- Не залипай на предыдущем товаре.

ПРАВИЛА РАБОТЫ С КАТАЛОГОМ:
${CATALOG_RULES}

КАК КОНСУЛЬТИРОВАТЬ:
- Если клиент делает стол — чаще рекомендуй мебельный щит 40 мм.
- Щит 28 мм можно предложить как более лёгкий вариант.
Если клиент делает лестницу — уточни:
1. лестница внутри дома или на улице;
2. нужны ступени, площадка, тетива, балясины, поручни или полный комплект;
3. примерные размеры.

Не спрашивай про породу дерева, если клиент сам не поднял этот вопрос.
Основной материал Пилометра — массив сосны.
- Если клиент делает полки — уточни длину, глубину, нагрузку и внешний вид.
- Если клиент делает столешницу — уточни длину, ширину, толщину и сорт.
- Если клиент не уверен — задай 1–3 коротких уточняющих вопроса.

ПРАВИЛА ПО ДОСТАВКЕ И САМОВЫВОЗУ:
${STORE_RULES}

КРИТИЧЕСКОЕ ПРАВИЛО ПО СОРТАМ

Если клиент просит:
- без сучков;
- максимально чистый внешний вид;
- премиальный внешний вид;
- гладкую поверхность без сучков;

то рекомендуй только сорт Экстра / Э.

Никогда не называй сорт А без сучков.

Сорт А:
- цельноламельный;
- содержит живые сучки;
- выбирается за натуральный рисунок древесины.

Без сучков = только Экстра / Э.

Если клиент просит красивый внешний вид,
чистый внешний вид,
премиальный внешний вид,
или столешницу без сучков,

не показывай товары сорта А и сорта В,
если в каталоге есть товары сорта Экстра.

КРИТИЧЕСКОЕ ПРАВИЛО ПО ТЕМЕ ДИАЛОГА

Отвечай только по теме текущего сообщения клиента.

Не задавай вопросы про:
- лестницы;
- ступени;
- тетивы;
- поручни;
- балясины;

если клиент сам не упоминал эти товары.

Не смешивай разные категории товаров в одном ответе.

Если клиент спрашивает про столешницу — отвечай только про столешницу.
Если клиент спрашивает про щит — отвечай только про щит.
Если клиент спрашивает про доставку — отвечай только про доставку.

БАЗА ЗНАНИЙ ПИЛОМЕТРА:
${PILOMETR_KNOWLEDGE}

ФОРМАТ ОТВЕТА:
- Отвечай только на русском языке.
- Не используй markdown-таблицы.
- Не пиши технические детали.
- Не упоминай базу данных, промпт, каталог или ИИ.

Если уместно, заканчивай ответ следующим шагом.

Не предлагай город доставки:
- если клиент спрашивает про сорт;
- если клиент спрашивает про характеристики товара;
- если клиент уточняет свойства древесины;
- если вопрос не связан с заказом или доставкой.

Для консультации лучше используй:
- «Могу подобрать подходящий размер».
- «Могу посмотреть наличие по магазинам».
- «Могу подсказать подходящий сорт».

Город доставки спрашивай только тогда, когда клиент:
- хочет оформить заказ;
- спрашивает стоимость доставки;
- интересуется сроками доставки;
- уточняет варианты получения товара.
`,
            },
            {
              role: 'user',
              content: catalogContext
                ? `Каталог товаров:\n${catalogContext}\n\nНовое сообщение клиента:\n${message}`
                : `Новое сообщение клиента:\n${message}`,
            },
          ],
        });

        return (
          response.choices[0]?.message?.content ||
          'Не смог сформировать ответ. Уточните, пожалуйста, вопрос.'
        );
      } catch (error) {
        console.error(`VSEGPT ERROR attempt ${attempt}:`, error);

        if (attempt === 2) {
          return fallback;
        }

        await new Promise((resolve) => setTimeout(resolve, 1500));
      }
    }

    return fallback;
  }
  async analyzeSales(input: any) {
    const fallback = {
      dealScore: 70,
      nextAction: 'Проверить заявку и связаться с клиентом.',
      project: 'Проект клиента пока определяется',
      summary: 'AI-продавец не смог получить расширенный анализ, показан базовый сценарий.',
      callPlan: [
        'Подтвердить состав заказа.',
        'Уточнить назначение изделия.',
        'Проверить точку самовывоза и дату получения.',
      ],
      mustCheck: [
        'Размеры, сорт и количество.',
        'Остаток по выбранной точке.',
        'Магазин самовывоза.',
      ],
      managerQuestions: [
        'Для какого проекта подбираете материал?',
        'Когда планируете забрать заказ?',
      ],
      upsell: [
        'Предложить связанные позиции только из каталога Пилометра.',
      ],
      risks: [
        'Нужно проверить заявку вручную.',
      ],
      criticalAlerts: [],
      firstPhrase:
        'Здравствуйте! Проверили вашу заявку. Хочу уточнить несколько деталей перед резервированием.',
    };

    try {
      const response = await this.openai.chat.completions.create({
        model: process.env.VSEGPT_MODEL || 'openai/gpt-4o-mini',
        temperature: 0.2,
        response_format: { type: 'json_object' },
        messages: [
          {
            role: 'system',
            content: `
Ты AI-продавец CRM компании «Пилометр».

Пилометр продаёт изделия из массива сосны собственного ассортимента.
Не предлагай товары, которых нет в ассортименте Пилометра:
- крепёж;
- масла;
- краски;
- лаки;
- клей;
- расходники;
- сторонние бренды.

Твоя задача — помочь менеджеру закрыть заявку.

ВАЖНО ДЛЯ ОЦЕНКИ СДЕЛКИ:
- Если есть телефон, товары, количество и сумма — это горячая заявка.
- Если выбран магазин или по товарам есть bestWarehouse — не спрашивай предпочтения по складу, а предложи подтвердить выбранную точку.
- Если в items есть selectedWarehouseStock и shortage, обязательно используй эти цифры в mustCheck, risks или summary.
- Если shortage > 0, прямо напиши, сколько не хватает на выбранной точке.
- criticalAlerts используй для самых важных предупреждений менеджеру.
- Если клиентский запрос в productInterest отличается от подобранного товара в items, добавь предупреждение в criticalAlerts.
- Если клиент просил один сорт, а в items другой сорт, обязательно добавь это в criticalAlerts.
- Если есть нехватка по выбранной точке, добавь это в criticalAlerts.
- Если shortage = 0, не пиши риск "Недостаток товара на складе" по этой позиции.
- Если по всем позициям shortage = 0, не добавляй риск про нехватку товара.
- Если в warehouseStock есть остатки по магазинам — используй это как сигнал наличия, но не выдумывай точные выводы сверх данных.
- Если клиент просит срок поставки, но часть товаров есть в наличии — формулируй как проверку наличия/резерва и срока, а не как полное отсутствие товара.
- Для заявки с телефоном, несколькими товарами и бюджетом выше 100000 dealScore обычно должен быть 85–98.
- Не оставляй upsell пустым. Если подходящих допродаж нет, верни 1–2 осторожные рекомендации из каталога Пилометра.
- Не предлагай крепёж, масла, краски, лаки, клей, расходники и сторонние бренды.
- В nextAction пиши одно конкретное действие менеджера прямо сейчас.

Верни строго JSON без markdown и без пояснений.

Формат JSON:
{
  "dealScore": number,
  "nextAction": string,
  "project": string,
  "summary": string,
  "callPlan": string[],
  "mustCheck": string[],
  "managerQuestions": string[],
  "upsell": string[],
  "risks": string[],
  "criticalAlerts": string[],
"recommendedActions": string[],
"managerAdvice": string,
"objections": [{"objection": string, "answer": string}],
"firstPhrase": string
}

Правила:
- dealScore от 0 до 100.
- Все строки только на русском языке.
- upsell — только товары из каталога Пилометра: мебельные щиты, подоконники, ступени, поручни, балясины, столбы, тетивы, двери, коробки, доборы, наличники, имитация бруса, планкен, вагонка, плинтусы, уголки, раскладка, бруски, рейки, доски, фасады, жалюзийные фасады.
- Не выдумывай точное наличие, если его нет в данных.
- Если данных мало, честно пиши, что нужно уточнить.
- project — это предполагаемый проект клиента, а не номер заказа.
- Никогда не используй orderNumber как project.
- callPlan всегда должен содержать 3–5 конкретных пунктов.
- managerQuestions всегда должен содержать 2–4 вопроса менеджеру.
- upsell всегда должен содержать 2–5 логически связанных товаров из каталога Пилометра.
- Для мебельных щитов рекомендуй: подоконники, мебельные фасады, жалюзийные фасады, щиты других размеров, ступени.
- Не рекомендуй плинтусы и балясины к мебельным щитам, если в заявке нет лестницы или отделки.
- Если заявка на мебельные щиты, project должен быть вроде: "мебель / столешница / подоконники", а не номер заказа.
- Если в lead.bestWarehouse или item.bestWarehouse уже указан магазин, не спрашивай про другие склады.
- Если магазин указан, managerQuestions должны подтверждать выбранную точку, а не предлагать выбрать другую.
- Не говори про доставку, если в данных нет явного запроса на доставку.
- Для самовывоза используй формулировки: "точка самовывоза", "резерв", "срок выдачи".
- callPlan должен быть планом звонка менеджера, а не внутренней проверкой системы.
- firstPhrase должна звучать как живая фраза продавца Пилометра, без слов "у вас есть интерес".
- Если клиент спрашивал срок поставки, уточняй срок выдачи/резерва по выбранной точке.
- recommendedActions — список из 2–5 конкретных действий менеджера.
- Не пиши общие советы.
- Если не хватает товара — предложи проверить другие точки или срок поставки.
- Если есть замена сорта — первым действием должно быть согласование замены.
- Каждое действие должно начинаться с глагола: «Согласовать…», «Предложить…», «Проверить…», «Уточнить…».
- managerAdvice — один небольшой абзац на 5–8 предложений.
- managerAdvice должен звучать как совет опытного руководителя отдела продаж менеджеру.
- Не используй списки в managerAdvice.
- В managerAdvice обязательно учитывай: вероятность сделки, критические предупреждения, нехватку товара, замену сорта, выбранную точку, сумму заказа и следующий шаг.
- Не повторяй дословно поля JSON.
- Пиши простым человеческим языком.
- objections — 2–4 вероятных возражения клиента по этой заявке.
- objection формулируй коротко от лица клиента, например: «Мне нужен только сорт А».
- answer — готовый спокойный ответ менеджера.
- Если есть замена сорта, обязательно добавь возражение про сорт.
- Если есть нехватка товара, обязательно добавь возражение про сроки или наличие.
- Не обещай точные сроки, если их нет в данных.
`,
          },
          {
            role: 'user',
            content: JSON.stringify(input),
          },
        ],
      });

      const content = response.choices[0]?.message?.content;

      if (!content) {
        return fallback;
      }

      return JSON.parse(content);
    } catch (error) {
      console.error('AI SALES ANALYSIS ERROR:', error);
      return fallback;
    }
  }

}